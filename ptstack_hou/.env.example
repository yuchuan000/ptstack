# 环境变量配置
# ======================================================

# 运行环境：development(开发) 或 production(生产)
NODE_ENV=development

# 服务监听端口
PORT=3000

# ======================================================
# 数据库连接配置
# ======================================================
# 数据库服务器地址
DB_HOST=localhost
# 数据库端口
DB_PORT=3306
# 数据库名
DB_NAME=ptstack_db
# 数据库用户名
DB_USER=root
# 数据库密码
DB_PASSWORD=

# ======================================================
# JWT 认证配置
# ======================================================
# JWT 密钥（生产环境请务必更换为强随机密钥！）
JWT_SECRET=your-secret-key-change-in-production
# Access Token 有效期（短期，用于 API 认证）
ACCESS_TOKEN_EXPIRES_IN=15m
# Refresh Token 有效期（长期，用于刷新 Access Token）
REFRESH_TOKEN_EXPIRES_IN=7d

# ======================================================
# 邮件服务配置
# ======================================================
# 邮箱地址（用于发送验证邮件）
EMAIL_USER=your-email@example.com
# SMTP授权码（不是登录密码）
EMAIL_PASS=
# SMTP服务器地址
SMTP_HOST=smtp.example.com
# SMTP端口（465为SSL加密端口）
SMTP_PORT=465
# 邮箱验证码有效期（单位：分钟）
EMAIL_VERIFICATION_CODE_EXPIRES_IN=15

# ======================================================
# ID 生成配置
# ======================================================
# ID格式模式说明：
# - 支持固定文本和占位符混合使用
# - 占位符格式：{类型}{长度}
#
# 支持的类型：
# - letter: 纯字母 (a-z, A-Z)
# - digit: 纯数字 (0-9)
# - symbol: 符号 (_-)
# - alphanumeric: 字母+数字
# - all: 字母+数字+符号
#
# 配置示例：
#
# 基础示例：
#   user_{digit}{12} → user_123456789012
#   article-{letter}{8} → article-AbCdEfGh
#
# 中间插入固定字符：
#   user-{letter}{2}-cs-{digit}{6} → user-Ab-cs-123456
#   art_{letter}{3}_{digit}{5} → art_Abc_12345
#
# 多段分隔：
#   {letter}{2}-{digit}{3}-{letter}{4} → Ab-123-CdEf
#   u-{digit}{4}-{digit}{2}-{digit}{4} → u-2025-02-1234
#
# 特殊格式：
#   {letter}{1}---{digit}{4}---{letter}{1} → A---1234---B
#   ID-{digit}{8} → ID-20250220
#   uid_{digit}{10} → uid_1234567890
#
# 纯随机（无固定前缀）：
#   {letter}{4}{digit}{2}{letter}{10} → AbCd12EfGhIjKlMn
USER_ID_PATTERN={digit}{9}
ARTICLE_ID_PATTERN={alphanumeric}{12}
ANNOUNCEMENT_ID_PATTERN={alphanumeric}{12}
